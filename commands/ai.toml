prompt = """
아래 한국어 글을 GEMINI.md 의 'AI 문체(ai)' 규칙으로 문장 단위로 다듬으세요.
첫째·둘째 기계적 나열, "결국/시사하는 바가 크다" 결말 공식, 과장 어휘, 이모지·과한 불릿/볼드,
비슷한 길이·종결 반복, 문두 접속사 남발, "것이다" 식 맺음을 자연스럽게 손봅니다. 말투(높임말/반말)는
그대로 둡니다. 한 문장씩 따로 읽고 다듬되, 뜻은 바꾸지 말고, 문장을 합치거나 나누거나 순서를 바꾸지 마세요.
고칠 게 없다고 판단한 부분도 다시 읽고 교정 누락이 있으면 고칩니다. 모호한 정보만 보존하고 주변 표현은 다듬습니다.
수치·고유명사·직접 인용·영어 약어·코드는 건드리지 않습니다. 제목·목록·표는 구조를 유지하고 텍스트를 다듬습니다.
중간점과 구분용 대시는 GEMINI.md의 보호 예외를 지키며 먼저 풀어 씁니다. 다른 교정 여부와 무관하게 제거 대상이 남으면 완료로 반환하지 않습니다. 나머지 기존 교정 순서는 유지합니다. 자동 검사를 실행하지 않았다면 수동 검토임을 밝힙니다.

결과: 바뀐 문장만 `전 → 후` 표 + 다듬은 전체 글 + 한 줄 요약(문장 N개 / 변경 k건).

입력 글:
{{args}}
"""
