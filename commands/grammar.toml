prompt = """
아래 한국어 글의 맞춤법과 문체를 국립국어원 기준으로 문장 단위로 교정하세요.
맞춤법은 예요/이에요, 되/돼, 안/않, 데/대, 든/던, 로서/로써, 율/률, 띄어쓰기 같은 오류를 바로잡고,
문체는 한 글에서 종결문체(해요체/합니다체/한다체)를 섞지 않게 통일하고, 사물존칭은 빼되 간접높임은
살립니다. 먼저 글 전체의 우세 문체를 정하세요. 한 문장씩 따로 읽고 다듬되, 뜻은 바꾸지 말고,
문장을 합치거나 나누거나 순서를 바꾸지 마세요. 수치·고유명사·직접 인용·코드는 건드리지 않습니다.

결과: 바뀐 문장만 `전 → 후` 표 + 다듬은 전체 글 + 한 줄 요약(문장 N개 / 변경 k건).

입력 글:
{{args}}
"""
