prompt = """
아래 한국어 글을 GEMINI.md 의 규칙에 따라 문장 단위로 첨삭·윤문하세요.
순서는 맞춤법 → 문장(번역투·군더더기) → AI 문체 → 문체입니다. 한 문장씩 따로 읽고 다듬되, 뜻은 바꾸지 말고,
긴 문장은 한 작업 단위 안에서 나눌 수 있지만 정보를 삭제·재배치하거나 작업 단위를 합치지 마세요.
고칠 게 없다고 판단한 부분도 마지막에 다시 읽고 교정 누락이 있으면 고칩니다. 모호한 정보만 보존하고 주변 표현은 다듬습니다.
수치·고유명사·직접 인용·영어 약어·코드는 건드리지 않습니다. 제목·목록·표는 구조를 유지하고 텍스트를 다듬습니다.
중간점과 구분용 대시는 GEMINI.md의 보호 예외를 지키며 반드시 풀어 씁니다. 자동 검사를 실행하지 않았다면 수동 검토임을 밝힙니다.

결과:
- 바뀐 문장만 `전 → 후` 표로 (적용한 규칙 종류 표시)
- 다듬은 전체 글
- 한 줄 요약: 문장 N개 / 변경 k건

입력 글:
{{args}}
"""
